# `@workspace/cli`

The **macOS usage collector**, a Rust binary. It reads local agent logs and
POSTs daily rows to the ingest endpoint. Install, OAuth login, and LaunchAgent:

**[macos/INSTALL.md](./macos/INSTALL.md)**
