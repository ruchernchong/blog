{
  "name": "@workspace/usage",
  "version": "1.15.0",
  "private": true,
  "scripts": {
    "login": "cargo run --release -- login",
    "measure": "cargo run --release -- measure",
    "ingest": "cargo run --release -- ingest"
  }
}
