{
  "name": "@workspace/usage",
  "version": "1.15.0",
  "private": true,
  "scripts": {
    "login": "cargo run --release --manifest-path rust/Cargo.toml -- login",
    "measure": "cargo run --release --manifest-path rust/Cargo.toml -- measure",
    "ingest": "cargo run --release --manifest-path rust/Cargo.toml -- ingest"
  }
}
